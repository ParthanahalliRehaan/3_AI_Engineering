# Tasks to do, to become Advanced(Project 3 MCP Swarm)
0. Learn all types of Transport(Classify which one is the best)?
1. Learn about creating a server with tools(gmail.send, so on other APIs but for local environment like file system access, database access), resources(local database, file system or remote database,github repo, so on) and prompts (Used for improving tools).
2. Learn inspecting the server(or servers)
3. Integrate with different types of Client(Claude code UI and CLI, Cursor and others)
4. Can a tool have multiple functionality?
5. Can we connect one server with multiple tools?
6. Can we use all types of transport in a single server
7. How to make MCP server remote?
# **More doubts to be stored, will be cleared during project 3**
